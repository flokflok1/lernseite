/**
 * Group Domain Types (GBA System)
 *
 * Re-exported from infrastructure (canonical source for API DTOs).
 * Presentation components import from here for convenience.
 */

export type {
  Group,
  GroupMember,
  GroupPermission,
  RegistryPermission
} from '@/infrastructure/api/clients/panel/admin/groups.types'
